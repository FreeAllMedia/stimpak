describe("(CLI) stimpak generators", () => {
	it("should throw an error if any of the generators aren't installed");
	it("should use the designated generators");
	it("should call the designated generators");
	it("should print out when it's done");
	it("should run multiple designated generators");
});
