import fileSystem from "fs";

export default class Generator {
	constructor(stimpak) {
		const filePath = `${stimpak.destination()}/generated1.js`;
		fileSystem.writeFileSync(filePath, "GENERATED!");
	}
}
