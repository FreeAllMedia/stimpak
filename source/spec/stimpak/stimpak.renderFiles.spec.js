import Stimpak from "../../lib/stimpak/stimpak.js";

describe("stimpak.renderFiles()", () => {
	it("should return itself to enable chaining");
	it("should render each template file from all source files using the answers as template values");
});
