import gulp from "gulp";

import paths from "../paths.json";

gulp.task("build-spec-json", ["build-lib"], () => {
	// return gulp
	// 	.src(paths.source.specJson)
	// 	.pipe(
	// 		gulp.dest(paths.build.directories.spec)
	// 	);
});
